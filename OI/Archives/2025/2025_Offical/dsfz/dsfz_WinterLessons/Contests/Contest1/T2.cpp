#pragma GCC optimize(2)
#pragma GCC optimize(3)
#include <bits/stdc++.h>
using namespace std;
int f[10101010];
int main() {
    long long a, b;
    cin >> a >> b;
    cout << (a - 1) * (b - 1) - 1;
}
