# pragma GCC optimize (3)
# pragma GCC optimize (" Ofast ")
# pragma GCC optimize (" inline ")
#include <bits/stdc++.h>
using namespace std;



int main() {
	int n, m, l;
	cin >> n >> m >> l;
	for (int i = 1; i <= m; i++) cin >> n;
	cout << l << endl;
}
