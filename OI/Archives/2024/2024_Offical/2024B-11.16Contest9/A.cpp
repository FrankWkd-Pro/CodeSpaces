# pragma GCC optimize (3)
# pragma GCC optimize (" Ofast ")
# pragma GCC optimize (" inline ")
#include <bits/stdc++.h>
using namespace std;

int main() {
	int T, m;
	cin >> T;
	while (T--) {
		int x, y, xx, yy;
		cin >> m >> x >> y >> xx >> yy;
	}
}
